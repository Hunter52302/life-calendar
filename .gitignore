# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit real keys
.env
.env.local
.env.*.local

# Vercel
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code session data
.claude
data/
.env

# Mobile app (Expo)
mobile/.env
mobile/.env.local
mobile/node_modules/
mobile/.expo/
mobile/dist/

# Shared package build artifacts
shared/dist/

# Capacitor native projects
# These are generated from `npm run cap:sync` — don't commit them.
# Uncomment if you want to commit the native projects for CI/CD:
# !ios
# !android
ios/
android/

# Tauri build artifacts (Rust compilation output — huge, regenerated by cargo)
src-tauri/target/
src-tauri/gen/

# Tauri signing keys — NEVER commit these
src-tauri/update-key
src-tauri/update-key.pub
